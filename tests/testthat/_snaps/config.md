# configuration rejects an unknown Defuddle backend

    Code
      rill_config()
    Condition
      Error in `normalize_defuddle_backend()`:
      ! Invalid Defuddle backend "browser".
      i Set `DEFUDDLE_BACKEND` to "hosted" or "local".
